// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // your config options here, e.g.
  images: {
    domains: ['example.com'],
  },
  // ... other settings
};

module.exports = nextConfig;